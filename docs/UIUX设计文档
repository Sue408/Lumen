## 项目简述

一个运行在本机的个人 LLM 网关：把多个上游提供商聚合成统一的本地接口，按模型别名把请求路由到对应的上游模型，并记录每一次调用的 token 与花费。

产品定位是一本私人的 AI 使用账本，不是控制台，不是仪表盘。它安静、可翻查、只服务一个人。

核心页面五个：用量统计、模型路由、虚拟密钥、上游提供商、使用日志，外加设置。

## 设计语言

### 核心设计

隐喻：账簿​。用量是总账，虚拟密钥是分户账，月度汇总是月结，用量预警是朱批，使用日志是流水，路由规则是食谱。

五条原则：

1. 纸是载体，不是主题​ —— 不做纹理、不做旧、不加噪点，只是纸白
2. 纵向长卷​ —— 从上往下读，靠留白分区，不靠卡片和分隔线
3. 数据文字化​ —— 能用一句话说清的绝不画图；先写那句话，再决定配不配图
4. 一个界面只有一个强调色​ —— 朱砂独占预警，绝不用于装饰，绝不进入图表色板
5. 图表是印刷品，不是控件​ —— 去脚手架，无网格线、无轴线、无图例块、无阴影渐变

反面参照：Grafana、云厂商控制台、Postman。共同点是信息密度压迫 + 蓝紫冷色 + 表格主导，本设计去掉这三样。

气质基调：暖纸白底、衬线标题配无衬线正文、1px 淡墨描边、4–6px 小圆角、矿物颜料低饱和配色、极淡纸层阴影。

### 设计规范

#### 色彩

- 纸白 #FBFAF8 不纯白，墨黑 #1C1A17 不中性灰黑，都带一丝暖
- 深色模式是"夜晚的纸" #1A1917 暖黑，不是纯黑
- 墨阶 5 级用同一色相的不同透明度表达强度，像墨渗在纸上；深色下方向反转，上限压到 .78
- 图表分类只用四种矿物颜料（赭石、靛青、苔绿、藤黄），低饱和带灰度
- 朱砂 #A8503E 三条铁律：只用于超预算 / 失败 / 已停用，绝不装饰，绝不进图表
- 深色模式下阴影失效，改用描边与明度差表达层级

#### 排版

- 标题与关键数字用衬线，标签与正文用无衬线，这个混排是印刷感来源
- 字阶比例 1.25，共 8 档（display 32 → caption 12，metric 40）
- 所有数字必须 tabular-nums，否则数字跳动时整行会抖
- 中西文混排手动加 1/4 空格；¥38.6 不加空格，412 次 加空格

#### 空间与形状

- 8pt 栅格：卡片间距 24，段落间距 64，内容区左右页边距 64，内容最大宽 960
- 圆角 2 / 4 / 6 / pill 四档，纸是裁切的不充气，不用 12px+ 大圆角
- 描边自成体系不跟栅格：1px 常规，2px 选中与聚焦环
- 纸层阴影极淡，只暗示"一张纸叠在另一张上"

#### 动效

- 100 / 150 / 250ms 三档，只用在展开折叠与悬停，不做装饰性动画
- 唯一例外：侧边栏底部"本机运行"呼吸圆点，情绪锚点不是功能入口
- 必须支持 prefers-reduced-motion 降级

#### 图表

- 去脚手架：只留关键刻度，标签直接标在图形旁
- 单色墨阶优先于彩虹色阶；多色时用矿物颜料不用原色
- 环形图扇区间留 2px 纸白缝，是裁切感的关键
- 强度映射用墨阶，用当前视图分位数分档，零值留白
- 全站唯一允许的图表浮层是 Tooltip，其余交互靠直接标注
- 不用图表库：纸感图表本质是做减法，而库的默认值全是要删的东西

#### 组件与状态

- 控件高度三档 28 / 32 / 40；图标 16 / 20 / 24，同一视图不混用
- 线性图标 stroke 1.5，圆头圆角，不用填充图标
- 交互态必须齐：hover / focus-visible / active / disabled / loading
- 状态必须齐：空、错误、部分失败、加载、超长、截断
- focus-visible 用 2px 赭石 outline，offset 2px
- 对比度正文 ≥ 4.5:1，大字与图形 ≥ 3:1；颜色不单独承载信息