# 参与 Lumen

Lumen 是一个个人开源项目。Issue 和 PR 都欢迎，但发布前优先保证范围稳定、边界诚实、用户数据安全。

## 提交 Issue

- Bug 请说明系统版本、Lumen 版本、复现步骤、预期结果和实际结果。
- 配置问题请附脱敏后的错误信息；不要提交 API Key、完整本地数据库或包含真实账单的截图。
- 功能建议请先说明具体工作流和当前痛点，不要只写“希望支持某平台”。
- 安全相关问题不要在公开 Issue 中披露细节，请先通过仓库所有者公开资料中的联系方式联系。

## 提交 Pull Request

- 一个 PR 只解决一个清晰问题，避免顺手重排无关代码。
- 遵循 `AGENTS.md` 的目录、样式和验证约定。
- 提交前至少运行 `pnpm test`、`pnpm build`；涉及 Rust 时同时运行 `cargo test` 与 `cargo clippy -- -D warnings`。
- 不要提交真实 API Key、数据库、账号信息、账单截图或未经脱敏的日志。
- 涉及行为变化时同步更新文档或测试。

## 范围原则

- 个人本机、本地优先、数据归属清晰。
- 不为宣传而夸大尚未验证的协议覆盖。
- 不主动引入服务器、Docker 或云服务依赖。
- 优先修复安装、配置、记账准确性与安全边界问题。